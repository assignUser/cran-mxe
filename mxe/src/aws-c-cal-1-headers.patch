diff --git a/CMakeLists.txt b/CMakeLists.txt
index d57a3fd..c4de4f5 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -56,9 +56,9 @@ if (WIN32)
             "source/windows/*.c"
         )
         if (AWS_SUPPORT_WIN7)
-            set(PLATFORM_LIBS BCrypt)
+            set(PLATFORM_LIBS bcrypt)
         else()
-            set(PLATFORM_LIBS NCrypt)
+
         endif()
     endif()
 
